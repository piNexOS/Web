@page
@model Web.Pages.CadUsuarios.DeleteModel

@{
    <!DOCTYPE html>
    <html lang="pt-br">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Excluir Usuário - NexOS</title>

        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

        <style>

            :root {
                --cor-fundo: #373F46;
                --cor-header: #272C2F;
                --cor-nexos: #26a5ee;
                --cor-nexosdark: rgb(17, 136, 206);
                --cor-texto: #fff;
                --cor-card: #2E343A;
                --cor-card-dark: #23282E;
                --cor-danger: #ff4d4d;
                --cor-danger-dark: #d93636;
            }

            body {
                margin: 0;
                padding: 0;
            }

            main {
                background: linear-gradient(15deg, rgba(41,41,41,1) -100%, rgba(58,66,72,1) 70%);
                font-family: "Poppins", sans-serif;
                color: var(--cor-texto);
                min-height: 100vh;
                padding: 2rem;
                display: flex;
                justify-content: center;
                align-items: flex-start;
            }

            .card-delete {
                background: var(--cor-card);
                padding: 2rem;
                width: 100%;
                max-width: 600px;
                border-radius: 12px;
                box-shadow: 0 0 12px rgba(0,0,0,0.45);
            }

            h1 {
                text-align: center;
                font-size: 2.3rem;
                margin-bottom: 1rem;
                text-shadow: 0 0 6px rgba(255,100,100,0.2);
            }

            h3 {
                text-align: center;
                font-weight: 600;
                margin-bottom: 1.5rem;
                color: var(--cor-danger);
            }

            dl dt {
                font-weight: 600;
                color: var(--cor-nexos);
            }

            dl dd {
                margin-bottom: 1rem;
                font-size: 1.05rem;
            }

            .btn-danger {
                width: 100%;
                background: var(--cor-danger);
                padding: .8rem;
                border: none;
                color: white;
                border-radius: 8px;
                font-size: 1.1rem;
                font-weight: 600;
                cursor: pointer;
                transition: .2s;
                margin-bottom: 1rem;
            }

                .btn-danger:hover {
                    background: var(--cor-danger-dark);
                }

            .back-link {
                display: block;
                text-align: center;
                margin-top: .5rem;
                color: var(--cor-nexos);
                font-weight: 600;
                text-decoration: none;
            }

                .back-link:hover {
                    color: var(--cor-nexosdark);
                }

        </style>
    </head>

    <body>
        <main>

            <div class="card-delete">

                <h1>Excluir Usuário</h1>

                <h3>Tem certeza que deseja excluir este cadastro?</h3>

                <dl>
                    <dt>@Html.DisplayNameFor(model => model.TabUsuarios.Nome)</dt>
                    <dd>@Model.TabUsuarios.Nome</dd>

                    <dt>@Html.DisplayNameFor(model => model.TabUsuarios.Matricula)</dt>
                    <dd>@Model.TabUsuarios.Matricula</dd>

                    <dt>@Html.DisplayNameFor(model => model.TabUsuarios.Cargo)</dt>
                    <dd>@Model.TabUsuarios.Cargo</dd>

                    <dt>@Html.DisplayNameFor(model => model.TabUsuarios.Senha)</dt>
                    <dd>@Model.TabUsuarios.Senha</dd>
                </dl>

                <form method="post">
                    <input type="hidden" asp-for="TabUsuarios.IdTabUsuarios" />

                    <button type="submit" class="btn-danger">Excluir Usuário</button>
                </form>

                <a asp-page="./Index" class="back-link">Voltar à Lista</a>

            </div>

        </main>
    </body>

    </html>
}
